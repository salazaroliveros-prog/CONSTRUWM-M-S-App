# Copy to .env.local (do NOT commit .env.local)

# Optional: base path for deployments under a subpath (e.g. GitHub Pages repo name)
# Example: VITE_BASE=/mi-repo/
VITE_BASE=/

# =====================
# Supabase (Frontend)
# =====================
# Supabase project URL (Settings -> API)
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co

# Supabase anon public key (Settings -> API)
VITE_SUPABASE_ANON_KEY=

# Single organization UUID (public.organizations.id)
VITE_ORG_ID=00000000-0000-0000-0000-000000000000

# Public portals (shared secrets used by Edge Functions)
# NOTE: These tokens are stored in the frontend for now; treat them as anti-abuse tokens, not strong security.
VITE_PORTAL_ATTENDANCE_TOKEN=change-me
VITE_PORTAL_APPLICATIONS_TOKEN=change-me

# Admin token for the HR admin Edge Function (private usage)
VITE_ADMIN_TOKEN=change-me

# =====================
# Gemini (Server)
# =====================
# Gemini API key (kept server-side in server/server.mjs; do NOT expose in frontend)
GEMINI_API_KEY=
